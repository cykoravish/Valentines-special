declare module "tailwindcss/lib/util/flattenColorPalette" {
  const flattenColorPalette: (palette: any) => any;
  export default flattenColorPalette;
}

declare module "*.mp3" {
  const src: string
  export default src
}
